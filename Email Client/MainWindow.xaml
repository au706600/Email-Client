<Window x:Name="Load_MainWindow" x:Class="Email_Client.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Email_Client"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="600">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border CornerRadius="7" Width="500" BorderThickness="2" Height="40" BorderBrush="Gray" Background="White" Margin="0,10,430,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="30"></ColumnDefinition>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" FontFamily="Segoe MDL2 Assets" Text="&#xE721;" VerticalAlignment="Center" Foreground="Gray" FontSize="14"></TextBlock>
                <TextBox x:Name="searchBox" Grid.Column="1" GotFocus="searchBox_GotFocus" LostFocus="searchBox_LostFocus"  Width="450" Height="20" VerticalAlignment="Center" BorderThickness="0" Background="Transparent" FontSize="14"/>
            </Grid>
        </Border>
        
        <Border Grid.Row="1" HorizontalAlignment="Right" VerticalAlignment="Stretch" BorderThickness="1" BorderBrush="WhiteSmoke" CornerRadius="7" Margin="0,0,30,15">
            <ListView ItemsSource="{Binding EmailEntries}" BorderThickness="0" Margin="0,10,0,0" Width="1200" ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.HorizontalScrollBarVisibility="Disabled" ScrollViewer.CanContentScroll="True" SelectedItem="{Binding SelectedEmail}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="20">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="200"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="{Binding Sender}" VerticalAlignment="Center"/>
                            <TextBlock Grid.Column="1" Text="{Binding Topic}" VerticalAlignment="Center" Margin="20,0,20,0" TextTrimming="CharacterEllipsis"/>
                            <TextBlock Grid.Column="2" Text="{Binding Date}" VerticalAlignment="Center" Margin="0,0,10,0" HorizontalAlignment="Right"/>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Border>
    </Grid>
</Window>
