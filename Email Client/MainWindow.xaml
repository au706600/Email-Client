<Window x:Name="Load_MainWindow" x:Class="Email_Client.MainWindow"
        Background="SeaShell"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Email_Client"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="600">
    <Grid>
      
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="180"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Border Grid.Row="0" Grid.ColumnSpan="2" CornerRadius="7" Width="500" BorderThickness="2" Height="40" BorderBrush="Gray" Background="White" Margin="0,10,420,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="30"></ColumnDefinition>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" FontFamily="Segoe MDL2 Assets" Text="&#xE721;" VerticalAlignment="Center" Foreground="Gray" FontSize="14"></TextBlock>
                <TextBox x:Name="searchBox" Grid.Column="1" Width="450" GotFocus="searchBox_GotFocus" LostFocus="searchBox_LostFocus" VerticalAlignment="Center" BorderThickness="0" Background="Transparent" FontSize="14"/>
            </Grid>
        </Border>

        <Border Grid.Row="1" Grid.Column="0" CornerRadius="7" BorderThickness="2" Padding="10" VerticalAlignment="Stretch">
            <Button x:Name="DrawCircleButton" Height="40" Width="120" Padding="10" BorderBrush="LightGray" VerticalAlignment="Top" Click="DrawCircleButton_Click">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE70F;" FontSize="14" Margin="0,0,6,0" />
                    <TextBlock Text="Write" FontSize="14" />
                </StackPanel>
            </Button>
        </Border>

        <Border Grid.Row="1" Grid.Column="1" BorderBrush="LightGray" Width="1100" BorderThickness="2" Background="white" CornerRadius="7" Padding="0,10,0,0">
            <ListView ItemsSource="{Binding EmailEntries}" BorderThickness="0" ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.HorizontalScrollBarVisibility="Disabled" SelectedItem="{Binding SelectedEmail}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="20">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="200"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="{Binding Sender}" VerticalAlignment="Center"/>
                            <TextBlock Grid.Column="1" Text="{Binding Topic}" VerticalAlignment="Center" Margin="20,0,20,0" TextTrimming="CharacterEllipsis"/>
                            <TextBlock Grid.Column="2" Text="{Binding Date}" VerticalAlignment="Center" Margin="0,0,10,0" HorizontalAlignment="Right"/>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Border>
    </Grid>
</Window>
