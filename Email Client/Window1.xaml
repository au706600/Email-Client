<Window x:Class="Email_Client.ViewEmailContent"
        Background="SeaShell"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        xmlns:local="clr-namespace:Email_Client"
        mc:Ignorable="d"
        Title="ViewEmailContent" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Button Click="Back_Click" Height="40" Width="40" Background="Transparent" BorderThickness="0" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="20">
            <Path 
                    Data="M 25,10 L 5,10 M 5,10 L 15,0 M 5,10 L 15,20" 
                    Stroke="Black"
                    StrokeThickness="2"
                    Fill="Transparent"
            />
        </Button>

        <Grid Grid.Row="1">
            <wv2:WebView2 x:Name="HtmlViewer" Visibility="Collapsed"></wv2:WebView2>
            <TextBox x:Name="TextViewer" IsReadOnly="True" TextWrapping="Wrap" Visibility="Collapsed" TextAlignment="Center" ></TextBox>
        </Grid>


    </Grid>
</Window>
